### String

1. split():转数组

### Array

1. slice(index, end): 分割
2. join(): 转字符串
3. slice(start,end): 数组分割
4. concat(): 数组拼接
5. forEach(): 循环数组 **没有返回值** 

3. sort(): sort((a, b) => a - b) 升序 sort((a, b) => b - a)降序

### 对象

1. map(): 遍历对象 **有返回值**

#### lodash篇

1. take(array,N) / takeRight(array,N): 取数组第一个到N个 / 数组最后一个取N个

### 日期转换

1. 时间戳和日期： 
  时间戳->日期： new Date(timestamp)
  日期->时间戳： date.valueof()
